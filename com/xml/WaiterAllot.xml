<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.baomidou.test.mapper.WaiterAllotMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.baomidou.test.entity.WaiterAllot">
        <id column="id" property="id" />
        <result column="userid" property="userid" />
        <result column="barid" property="barid" />
        <result column="waiterid" property="waiterid" />
        <result column="creettime" property="creettime" />
        <result column="overtime" property="overtime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, userid, barid, waiterid, creettime, overtime
    </sql>

</mapper>
