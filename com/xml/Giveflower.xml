<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.baomidou.test.mapper.GiveflowerMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.baomidou.test.entity.Giveflower">
        <id column="id" property="id" />
        <result column="barid" property="barid" />
        <result column="userid" property="userid" />
        <result column="wxpaymoney" property="wxpaymoney" />
        <result column="leavemsg" property="leavemsg" />
        <result column="paystate" property="paystate" />
        <result column="paytime" property="paytime" />
        <result column="payid" property="payid" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, barid, userid, wxpaymoney, leavemsg, paystate, paytime, payid
    </sql>

</mapper>
