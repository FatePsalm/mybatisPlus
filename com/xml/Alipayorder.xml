<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.baomidou.test.mapper.AlipayorderMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.baomidou.test.entity.Alipayorder">
        <id column="id" property="id" />
        <result column="barid" property="barid" />
        <result column="applytime" property="applytime" />
        <result column="paymoney" property="paymoney" />
        <result column="paystate" property="paystate" />
        <result column="paytime" property="paytime" />
        <result column="month" property="month" />
        <result column="givemonth" property="givemonth" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, barid, applytime, paymoney, paystate, paytime, month, givemonth
    </sql>

</mapper>
