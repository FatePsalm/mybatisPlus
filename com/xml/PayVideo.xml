<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.baomidou.test.mapper.PayVideoMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.baomidou.test.entity.PayVideo">
        <id column="id" property="id" />
        <result column="userid" property="userid" />
        <result column="vid" property="vid" />
        <result column="payid" property="payid" />
        <result column="price" property="price" />
        <result column="barid" property="barid" />
        <result column="tid" property="tid" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, userid, vid, payid, price, barid, tid
    </sql>

</mapper>
